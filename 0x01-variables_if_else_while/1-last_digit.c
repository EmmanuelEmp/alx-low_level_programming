#include <stdio.h>
#include <time.h>
#include <stdib.h>
/**
 * main - determines either greater than 5, is less than 6 or is 0
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
        int n, 1;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	1 = n % 10;
	if (
}
